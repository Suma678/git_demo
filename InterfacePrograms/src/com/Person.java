package com;

public interface Person {

	int id = 101; //internally variable (id) will be automatically public, static and final in nature
	
	void eat(); // implicitly abstract and public void eat();
}
